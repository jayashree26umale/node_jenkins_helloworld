pipeline{
    agent any
    stages{
        steps("build")
        {
            sh "npm install"
        }
        steps("test")
        {
            sh "npm run test"
        }
        steps("deploy")
        {
            sh "node index.js"
        }
    }
}
