pipeline{
    agent any
    stages{
        stage('build'){
            steps{
                 sh "rm -rf node_modules && npm install"
            }
        }
        stage('test'){
            steps{
                 sh "npm run test"
            }
        }
        stage('deploy'){
            steps{
                 sh "node index.js"
            }
        }
    }
}
